@model IEnumerable<AjaxMVCCoreCRUD.Models.User>
@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>
@*<div class="row">
    <div class="col-md-12">
        <h2>Registered User</h2>

        @if (Model.Any())
        {
            <table class="table">
                <tr>
                    <th>FirstName</th>
                    <th>LastName</th>
                    <th>UserName</th>
                    <th>Email</th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.FirstName</td>
                        <td>@item.LastName</td>
                        <td>@item.UserName</td>
                        <td>@item.Email</td>
                    </tr>
                }
            </table>
        }
        else
        {
            <p>Thre is no registered user!</p>
        }


    </div>
</div>*@


<p>Use this page to detail your site's privacy policy.</p>
